Implement the UI with Gumroad-inspired design — minimal, bold, professional.

Design system:
- Color palette: near-black (#1A1A1A) + white + accent (#FF6154) + per-board accent colors
- Generous whitespace, large bold headings
- Rounded corners (rounded-xl, rounded-2xl), soft shadows
- Black primary buttons, bordered secondary buttons

Pages to implement:
- Auth pages (clean centered forms)
- Home page (board grid with colored cards, attendance banner)
- Board page (white bg, accent strip, centered lists)
- Inbox (split panel — conversation list + chat thread)
- Dashboard (stats cards + history table)
- Settings (Discord integration card)

Constraints:
- Tailwind CSS v4 only
- Clean reusable components
- Mobile-first, fully responsive
- Accessible (labels, aria, focus rings)
