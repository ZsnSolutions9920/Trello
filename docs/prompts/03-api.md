Define all API endpoints for the Employee Management Platform.

Include:
- Auth endpoints (register, login, me)
- Team management (create, invite, join)
- Board CRUD (with employee assignment)
- List CRUD
- Card CRUD + move (drag-and-drop)
- Comments
- Attendance (clock in/out, history)
- Messaging (send, inbox, reply)
- Discord OAuth (connect, disconnect)

For each endpoint:
- Method + route
- Auth requirement (public, auth, admin-only)
- Request body
- Response format
- Error cases
- Access control rules

Use RESTful design. Do NOT implement yet.
